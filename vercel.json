{
  "buildCommand": "bunx vite build && node scripts/generate-vercel-index.mjs",
  "outputDirectory": "build/client",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}
